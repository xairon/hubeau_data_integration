# Dagster Orchestrator dependencies (NO user code)
# These are the packages needed by dagster-webserver and dagster-daemon.
# User code lives exclusively in the worker (code location server).
dagster==1.11.14
dagster-webserver==1.11.14
dagster-postgres==0.27.14
