# Hub'Eau Data Integration Pipeline
# Dépendances Dagster ORCHESTRATOR UNIQUEMENT (image légère)

# Core Dagster
dagster==1.8.8
dagster-webserver==1.8.8
dagster-postgres==0.24.8
dagster-dlt==0.23.8  # ✅ ACTIVÉ - Integration native Dagster-DLT

# Base de données (connection metadata only)
psycopg[binary]==3.1.13

# HTTP minimal
httpx==0.26.0

# CLI
click>=8.0.0
pyyaml>=6.0

# Utilitaires
python-dotenv==1.0.0
pydantic==2.5.3

# Monitoring et logs
structlog==23.2.0
prometheus-client==0.19.0  # ✅ ACTIVÉ - Métriques Prometheus

# DLT minimal (pour dagster-dlt, mais pas de destinations lourdes)
dlt==0.4.12
