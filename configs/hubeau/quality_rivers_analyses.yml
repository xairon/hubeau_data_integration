resource:
  name: quality_rivers_analyses_raw
  endpoint: /analyse_pc.csv
  base_url: https://hubeau.eaufrance.fr/api/v2/qualite_rivieres
  # Bronze layer: no PK/FK constraints, duplicates allowed
  primary_key: null
extraction:
  default_params: {}
performance:
  parallelism: 1
  batch_size: 5000
  retry_times: 3
  retry_delay: 2.0
  rate_limit: 0.3
