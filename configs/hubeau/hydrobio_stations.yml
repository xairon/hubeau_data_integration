source:
  name: hydrobio
  max_table_nesting: 2
resource:
  name: hydrobio_stations
  endpoint: /stations_hydrobio
  base_url: https://hubeau.eaufrance.fr/api/v1/hydrobio
  primary_key: []
  write_disposition: replace
  columns:
    code_station:
      data_type: text
      nullable: false
    libelle_station:
      data_type: text
      nullable: true
    latitude:
      data_type: double
      nullable: true
    longitude:
      data_type: double
      nullable: true
extraction:
  slicing_mode: global
  pagination:
    type: page_based
  default_params: {}
performance:
  parallelism: 5
  chunk_size: 10000
  retry_times: 3
  retry_delay: 2.0
  rate_limit: 2.0
transformers:
- validate_primary_keys
- normalize_dates:
    fields:
    - date_ouverture
    - date_fermeture
    - date_maj
- validate_coordinates:
    lat_field: latitude
    lon_field: longitude
destinations:
  postgres:
    dataset_name: bronze
    schema_name: hydrobio
    create_indexes: true
  filesystem:
    bucket_url: s3://bronze
    file_format: parquet
    layout: '{table_name}/year={YYYY}/{load_id}.parquet'
  duckdb:
    database: data/hydrobio.duckdb
