resource:
  name: piezometry_stations_raw
  endpoint: /stations.csv
  base_url: https://hubeau.eaufrance.fr/api/v1/niveaux_nappes
  # Bronze layer: replace = full refresh (pas de doublons car table remplacée)
  primary_key: null
extraction:
  default_params: {}
performance:
  parallelism: 1
  batch_size: 5000
  retry_times: 3
  retry_delay: 2.0
  rate_limit: 0.3
