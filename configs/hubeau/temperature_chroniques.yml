resource:
  name: temperature_chroniques_raw
  endpoint: /chronique.csv
  base_url: https://hubeau.eaufrance.fr/api/v1/temperature
  # Bronze layer: no PK/FK constraints, duplicates allowed
  primary_key: null
extraction:
  default_params: {}
performance:
  parallelism: 1
  batch_size: 5000
  retry_times: 5
  retry_delay: 2.0
  rate_limit: 0.3
